import React from "react";

export default function Export() {
  return (
    <div className="error-container">
      <h1
        className="error-heading"
        style={{ color: "#fff", textAlign: "center" }}
      >
        Book not found, try a different search
      </h1>
    </div>
  );
}
